[build]
builder = "nixpacks"
buildCommand = "mvn clean package -DskipTests -B"

[deploy]
startCommand = "java -jar target/smart-mechanic-api-1.0.0.jar"
healthcheckPath = "/api/v1/actuator/health"
healthcheckTimeout = 120
